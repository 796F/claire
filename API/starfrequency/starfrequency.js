// var basic = require('basic');

SF = {};

//get star freq of a particular repository
SF.starFrequency = function(repository_owner, repository_name, T) {
  var star_freq_results = basic.frequency(data, T, function(ev) {

  });
}

module.exports = SF;
